[package]
name = "latencydesk-platform-windows"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
latencydesk-frame = { path = "../frame" }

latencydesk-h264 = { path = "../h264" }
latencydesk-input = { path = "../input" }
latencydesk-media = { path = "../media" }
latencydesk-platform = { path = "../platform" }
latencydesk-surface = { path = "../surface" }

[target.'cfg(windows)'.dependencies]
cxx = "=1.0.199"

[build-dependencies]
cxx-build = "=1.0.199"

[lints]
workspace = true
